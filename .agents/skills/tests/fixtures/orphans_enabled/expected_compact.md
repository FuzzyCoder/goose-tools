# orphans_enabled.json — Violations by Rule

| Rule | Errors | Advisories |
|---|---|---|
| orphans | — | 2 |
| **Total** | **0** | **2** |

(Note: `complexity.cyclomatic` have 0 violations.
The summary `violation_count` (0) counts only `severity: "error"` entries; `orphans` violations are `severity: "warning"` and tracked separately as advisories.)
